"""Python diagnostics that read the same settings the MATLAB pipeline reads.

An explicit package rather than a namespace one: the workspace outside this
clone has its own scripts/analysis, and two namespace packages of the same
name on sys.path would merge into one.
"""
